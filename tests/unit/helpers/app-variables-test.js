
import { appVariables } from 'todo-calor/helpers/app-variables';
import { module, test } from 'qunit';

module('Unit | Helper | app variables');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = appVariables([42]);
  assert.ok(result);
});

